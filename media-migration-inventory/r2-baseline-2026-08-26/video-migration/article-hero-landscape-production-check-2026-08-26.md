# 横向专用顶部图生产视觉核验

生产文章已输出横向专用顶部图的正式 v2 URL：

`https://images.houseplus-ch.com/media/houseplus-portable-power-supply-article-hero-landscape/`

页面 DOM 的替代文本、图片 URL 与数据库资产记录均为该新资源。完整容器核验确认图片自然尺寸为 2560 × 1440，网页展示尺寸为 1144 × 644、比例为 16:9，CSS 为 `object-fit: contain` 和居中定位；因此网页不会裁切该横向专用图。生产视图已确认 Logo、产品说明、两组二维码及标签、左侧便携电源和横幅下边缘均位于同一完整图像中。
